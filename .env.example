# App
PORT=8080
NODE_ENV=development

# Database / cache
DATABASE_URL=postgresql://kib:kib_password@localhost:55432/kib_movies?schema=public
REDIS_URL=redis://localhost:6379

# TMDB — create a free account at https://www.themoviedb.org and grab an API
# Read Access Token (v4) from Settings → API. The v3 key is optional.
TMDB_BASE_URL=https://api.themoviedb.org/3
TMDB_API_TOKEN=your_tmdb_v4_read_access_token
TMDB_API_KEY=your_tmdb_v3_api_key

# JWT (Phase 9)
# JWT_SECRET=
